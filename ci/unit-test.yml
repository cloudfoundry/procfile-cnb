---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: golang
    tag: latest

inputs:
- name: procfile-buildpack

caches:
- path: go-module-cache

run:
  path: procfile-buildpack/ci/unit-test.sh
