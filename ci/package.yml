---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: golang
    tag: latest

inputs:
- name: procfile-buildpack

outputs:
- name: artifactory

caches:
- path: go-module-cache

run:
  path: procfile-buildpack/ci/package.sh
